checkformat:clangformat:
  stage: checkformat
  image: ubuntu:23.10
  before_script:
    - apt-get update -y
    - apt-get install -y --no-install-recommends git clang-format-17
  script:
    - git clang-format-17 --diff
  only:
    - merge_requests
